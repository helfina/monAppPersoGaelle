<form method="post" action="{{ path('app_mois_delete', {'id': moi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moi.id) }}">
    <button class="btn">Delete</button>
</form>
